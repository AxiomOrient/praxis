# Public Runtime Contract Convergence Tasks

| ID | Status | Goal | Scope | Done When | Evidence | Depends On |
|----|--------|------|-------|-----------|----------|------------|
| PRC-01 | done | Freeze the public runtime contract boundary | `README.md`, `specs/03-SPEC.md`, `specs/04-RUNTIME-TARGET-PROFILES.md`, `specs/07-UX-IA.md`, `specs/00-HANDOFF.md` | Canonical docs describe only Codex and Claude as managed runtime-file targets and use `Agent Files` terminology consistently | Updated README/specs to remove Gemini runtime-file paths and ids from the active contract, replaced `guide` contract nouns with `agent file` terminology, and confirmed `rg -n "guide outputs|guide merge|\\bguides\\b|\\bGuidance\\b|GeminiNative|CodexGeminiSharedOpenStandard|GeminiUserRoot|GeminiProjectRoot|\\.gemini/skills|GEMINI.md" README.md specs/00-HANDOFF.md specs/03-SPEC.md specs/04-RUNTIME-TARGET-PROFILES.md specs/07-UX-IA.md` returns clean | - |
| PRC-02 | done | Remove Gemini from public runtime-file types | `crates/praxis-core/src/model.rs`, `apps/praxis-desktop/src/lib/types.ts` | Public enums, slots, target profiles, and snapshot DTOs no longer expose Gemini runtime-file contract variants or fields | Removed Gemini from `Agent`, `TargetProfile`, `AgentFileSlot`, `TargetPaths`, and `PlanSummary` in `model.rs`, mirrored the contract in desktop `types.ts`, and confirmed `rg -n "GeminiNative|CodexGeminiSharedOpenStandard|GeminiUserRoot|GeminiProjectRoot|gemini_skills|gemini_project_root" crates/praxis-core/src/model.rs apps/praxis-desktop/src/lib/types.ts` is clean | PRC-01 |
| PRC-03 | done | Remove Gemini runtime-file handling from managed workspace code | `crates/praxis-core/src/workspace.rs`, `crates/praxis-core/src/agent_files.rs`, `crates/praxis-core/src/source.rs`, `crates/praxis-core/src/manager.rs`, related test helpers in `create.rs`, `evaluation.rs`, `jobs.rs`, `library.rs` | Managed workspace setup, slot resolution, scanning, planning, apply flows, and their fixtures operate only on Codex and Claude runtime-file targets | Removed Gemini workspace paths, slot resolution, planner summary fields, and source discovery branches from managed runtime code, updated fixture constructors in `create.rs` / `evaluation.rs` / `jobs.rs` / `library.rs`, and confirmed `rg -n "Agent::Gemini|GeminiNative|CodexGeminiSharedOpenStandard|GeminiUserRoot|GeminiProjectRoot|gemini_project_root|gemini_skills" crates apps` returns clean while `cargo test --workspace -q` passes | PRC-02 |
| PRC-04 | done | Converge public command and bridge naming on `Agent Files` | `crates/praxis-cli/src/main.rs`, `apps/praxis-desktop/src-tauri/src/lib.rs`, `apps/praxis-desktop/src/lib/api.ts`, `apps/praxis-desktop/src/App.svelte`, `apps/praxis-desktop/src/lib/i18n/*.ts`, `apps/praxis-desktop/index.html`, public-facing strings in `crates/praxis-core/src/source.rs` | CLI help, desktop bridge/API names, UI copy, metadata copy, and i18n keys use `Agent Files` instead of `guidance`/`guides` | Renamed CLI `Guidance` to `AgentFiles`, Tauri/API commands to `agent_files` / `agent_files_write`, converged App + i18n key namespaces on `agentFiles` / `agentFileEditor`, and confirmed `rg -n "guide outputs|guide merge|Guidance\\b|guidance_write|guidance\\(|nav\\.guides|hero\\.guides|common\\.guides|plan\\.guides|guides\\.slot|guides\\.noPath" README.md specs crates apps --glob '!specs/99-REFERENCES.md'` returns clean | PRC-01, PRC-02 |
| PRC-05 | done | Preserve only bounded input compatibility | `crates/praxis-core/src/model.rs`, related tests | Legacy persisted inputs such as `guides` still deserialize where intentionally supported, but canonical outputs remain modernized | Kept only `#[serde(alias = "guides")]` on `InstallSelection.agent_file_templates` and added `install_selection_deserializes_legacy_guides_alias` plus `install_selection_serializes_canonical_agent_file_templates_key` so legacy input still reads while serialized output omits `guides` | PRC-02 |
| PRC-06 | done | Lock the converged contract with regression gates | `crates/praxis-core`, `apps/praxis-desktop`, repo root grep/build checks | Tests pass, desktop builds, and grep gates prove Gemini runtime-file contract and legacy public nouns no longer leak | `cargo test --workspace -q` passed, `npm run build` passed in `apps/praxis-desktop`, targeted public-noun grep passed clean, Gemini-contract grep passed clean, and Tauri serialization tests now assert current contract keys without Gemini runtime-file fields | PRC-03, PRC-04, PRC-05 |
