## Demo source guidance

Use the installed skills when they match the current task.

### Suggested modes

- Use `/plan-cleanly` before implementation when scope is unclear.
- Use `/debug-root-cause` when a fix is unclear or repeated.
- Use `/ship-checklist` before pushing or opening a PR.
