{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default-capability",
  "description": "Default desktop capability for the main window, including dialog access for workspace picking.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
